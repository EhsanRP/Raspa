kind: ExternalService
name: raspa
spec:
  image_pull_policy: Always
  port: 80
  source:
    context: .
    jar_name: LanguageDesign-0.0.1-SNAPSHOT.jar
    jdk_version: '11'
    project_type: spring_boot
